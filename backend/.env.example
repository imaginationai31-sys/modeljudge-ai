PORT=8787
# Leave unset for local JSONL storage.
DATABASE_URL=
# Set to false only for a local PostgreSQL server without TLS.
DATABASE_SSL=true
# Restrict browser access in production. Example: https://app.example.com
CORS_ORIGIN=http://localhost:8000

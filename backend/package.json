{
  "name": "modeljudge-ai-api",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first API for ModelJudge AI human preference evaluations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}

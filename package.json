{
  "name": "modeljudge-ai",
  "version": "0.9.2",
  "private": true,
  "description": "ModelJudge AI evaluation and dataset platform",
  "scripts": {
    "start": "node scripts/start.js",
    "test": "node --test tests/*.test.js",
    "validate": "node scripts/validate-dataset.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
